{
  "name": "pdfloveme-pdf-tests",
  "private": true,
  "type": "module",
  "description": "Test harness for js/fill-pdf-core.js and pages/fill-form.html. Dev-only — the site itself ships no npm dependencies; everything it needs is vendored under /vendor.",
  "scripts": {
    "fixtures": "node make-fixtures.mjs",
    "test:core": "node run-tests.mjs",
    "test:coords": "node coords.test.mjs",
    "test:e2e": "node e2e.test.mjs",
    "test:e2e:control": "node e2e.test.mjs --break",
    "test": "node run-tests.mjs && node coords.test.mjs && node e2e.test.mjs"
  },
  "devDependencies": {
    "@pdf-lib/fontkit": "1.1.1",
    "pdf-lib": "1.17.1",
    "pdfjs-dist": "3.11.174",
    "playwright": "^1.62.1"
  }
}
